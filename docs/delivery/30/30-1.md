[Back to task list](./tasks.md)

# [30-1] Upgrade dependencies for Expo SDK 55

## Description
Upgrade core dependencies from Expo SDK 54 to SDK 55, align React/React Native packages, and fix compatibility issues discovered during migration.

## Status History

| Timestamp | Event Type | From Status | To Status | Details | User |
| :-- | :-- | :-- | :-- | :-- | :-- |
| 2026-03-02 00:00:00 | Created | N/A | Proposed | Task created to execute SDK 55 dependency alignment and migration fixes. | ai_agent |
| 2026-03-02 00:01:00 | Status Change | Proposed | Agreed | User requested full Expo 55 research + implementation with React upgrade. | user |
| 2026-03-02 00:07:00 | Status Change | Agreed | InProgress | Started official-doc research, dependency upgrades, and compatibility audit. | ai_agent |
| 2026-03-02 00:58:00 | Status Change | InProgress | Review | Completed dependency alignment, fixed TS compatibility issue, and documented findings. | ai_agent |

## Requirements
- Upgrade `expo` to SDK 55-compatible version and align Expo-managed dependencies.
- Upgrade `react`/`react-dom` and `react-native` to SDK 55-compatible versions.
- Apply migration-safe config updates where applicable.
- Document source-backed findings and package guidance.

## Implementation Plan
1. Research official Expo/React/React Native upgrade references.
2. Upgrade Expo SDK and dependent packages to Expo-recommended versions.
3. Re-run compatibility checks and address migration regressions.
4. Record decisions, source links, and issues in dedicated migration docs.

## Test Plan
- Run `npx expo install --check` to verify dependency compatibility.
- Run `yarn tsc --noEmit` to catch type regressions.
- Run `yarn jest --ci` for unit/integration safety checks.
- Run `npx expo export --platform web --output-dir dist-web` for bundling validation.

## Verification
- `npx expo install --check` reports dependencies up to date (offline warning only).
- `yarn tsc --noEmit` passes after updating theme helper typing for RN 0.83 `ColorSchemeName`.
- `yarn jest --ci` passes all suites (`3/3`).
- `npx expo export --platform web --output-dir dist-web` succeeds.
- `npx expo-doctor` could not run in this environment due npm registry resolution restrictions.

## Files Modified
- `package.json`
- `yarn.lock`
- `app.json`
- `src/theme/index.ts`
- `docs/delivery/30/30-1-upgrade-findings.md`
- `docs/delivery/30/30-1-expo-guide.md`
- `docs/delivery/30/30-1-react-guide.md`
- `docs/delivery/30/30-1-react-native-guide.md`
- `docs/delivery/30/30-1-expo-router-guide.md`
- `docs/delivery/30/30-1-react-native-reanimated-guide.md`

