[Back to task list](./tasks.md)

# [30-2] E2E CoS Test Plan + execution

## Description
Define and execute the PBI 30 verification plan to confirm the SDK 55 migration satisfies the Conditions of Satisfaction.

## Status History

| Timestamp | Event Type | From Status | To Status | Details | User |
| :-- | :-- | :-- | :-- | :-- | :-- |
| 2026-03-02 00:50:00 | Created | N/A | Proposed | Task created to capture and execute PBI 30 validation checks. | ai_agent |
| 2026-03-02 00:51:00 | Status Change | Proposed | Agreed | Validation task approved as part of the requested upgrade implementation. | user |
| 2026-03-02 00:52:00 | Status Change | Agreed | InProgress | Began running verification commands and collecting outcomes. | ai_agent |
| 2026-03-02 00:58:00 | Status Change | InProgress | Review | Completed verification run and documented results/limitations. | ai_agent |
| 2026-03-02 21:29:00 | Status Change | Review | InProgress | Reopened validation after user requested Android device retry and Java/Gradle issue resolution. | user |
| 2026-03-02 21:58:00 | Significant Update | InProgress | InProgress | Hardened device smoke script to use Gradle release assembly + explicit `adb -s` installation path while validating on-device behavior. | ai_agent |
| 2026-03-02 22:30:00 | Status Change | InProgress | Review | Resolved navigator registration runtime issue and completed on-device gameplay verification with agent-device. | ai_agent |
| 2026-03-11 11:50:30 | Significant Update | Review | InProgress | Reopened validation to rerun Expo/CocoaPods health checks and a fresh iOS simulator native build after the newly reported native issue. | user |
| 2026-03-11 13:46:09 | Status Change | InProgress | Review | Revalidated the iOS native build path and installed/launched the updated app on the booted simulator. | ai_agent |
| 2026-03-11 13:56:09 | Significant Update | Review | InProgress | Reopened after user reported the simulator app quit unexpectedly and requested a clean install after confirming the correct Expo-native remediation path. | user |
| 2026-03-11 14:44:38 | Status Change | InProgress | Review | Completed a clean simulator rebuild on `main`, installed the app, relaunched it against Metro on port 8081, and verified gameplay interaction with `agent-device`. | ai_agent |

## Requirements
- Verify dependency compatibility state for SDK 55.
- Verify compile/test/build pathways relevant to this migration.
- Record pass/fail results and blockers.

## Implementation Plan
1. Execute dependency compatibility check.
2. Run TypeScript and Jest suites.
3. Run web export build verification.
4. Document outputs and unresolved environment limitations.

## Test Plan
- `npx expo install --check`
- `yarn tsc --noEmit`
- `yarn jest --ci`
- `npx expo export --platform web --output-dir dist-web`
- `npx expo-doctor` (if environment permits)

## Verification
- `npx expo-doctor` → pass (`17/17 checks passed. No issues detected!`) after aligning `expo` to `~55.0.5` and `expo-router` to `~55.0.4`.
- `cd ios && pod install` → pass, updating native Expo pods to the matching SDK 55 patch set before rerunning the simulator build.
- `npx expo install --check` → pass (`Dependencies are up to date` in offline mode after aligning `@react-navigation/native` range to Expo expectation).
- `yarn tsc --noEmit` → pass (after typed fix in `resolveThemeName`).
- `yarn jest --ci` → pass (`3` test suites).
- `npx expo export --platform web --output-dir dist-web` → pass.
- `xcodebuild -workspace ios/Soli.xcworkspace -scheme Soli -configuration Debug -sdk iphonesimulator -destination 'platform=iOS Simulator,id=28F666FA-BEB6-4BDF-96CC-F8273FF8B4C1' -derivedDataPath /tmp/soli-derived build` → pass (`** BUILD SUCCEEDED **`).
- `xcrun simctl install 28F666FA-BEB6-4BDF-96CC-F8273FF8B4C1 /tmp/soli-derived/Build/Products/Debug-iphonesimulator/Soli.app` → pass.
- `xcrun simctl launch 28F666FA-BEB6-4BDF-96CC-F8273FF8B4C1 ch.karimattia.soli` → pass (`pid 30630`).
- `xcodebuild -workspace ios/Soli.xcworkspace -scheme Soli -configuration Debug -sdk iphonesimulator -destination 'platform=iOS Simulator,id=28F666FA-BEB6-4BDF-96CC-F8273FF8B4C1' -derivedDataPath /tmp/soli-main-clean ONLY_ACTIVE_ARCH=YES ARCHS=arm64 EXCLUDED_ARCHS=x86_64 clean build` → pass (`** BUILD SUCCEEDED **`) from the committed `main` baseline.
- `xcrun simctl install 28F666FA-BEB6-4BDF-96CC-F8273FF8B4C1 /tmp/soli-main-clean/Build/Products/Debug-iphonesimulator/Soli.app` → pass.
- `npx expo start --dev-client --clear --port 8081` → pass; Metro bundled `node_modules/expo-router/entry.js` for iOS.
- `npx --yes agent-device@0.7.12 snapshot -i --platform ios` → pass; board snapshot captured on the running simulator app.
- `npx --yes agent-device@0.7.12 press @e10 --platform ios` followed by `Deal Again` confirmation → pass; board state changed and the in-game `Undo` affordance became visible.
- `npx expo-doctor` → blocked by environment npm network resolution (`ENOTFOUND registry.npmjs.org`).
- Android device validation reopened for release build + smoke run after reported Java/Gradle issues.
- `/Users/karim/Library/Android/sdk/platform-tools/adb devices` → connected Android device detected.
- `node .yarn/releases/yarn-4.5.0.cjs why @react-navigation/native` → single resolved runtime version (`7.1.31`) shared by app + `expo-router`.
- `./gradlew app:assembleRelease --no-daemon --console=plain` (from `android/`) → pass (`BUILD SUCCESSFUL`).
- `adb -s <serial> install -r android/app/build/outputs/apk/release/app-release.apk` → pass (`Success`) on `192.168.1.12:37635`.
- `adb -s <serial> shell monkey -p ch.karimattia.soli -c android.intent.category.LAUNCHER 1` → app launch pass.
- `npx -y agent-device open ch.karimattia.soli --platform android --serial 192.168.1.12:37635 --relaunch --json` → pass.
- `npx -y agent-device screenshot .agents/skills/agent-device-after-fix.png --platform android --serial 192.168.1.12:37635 --json` → captured playable board UI (navigator error screen no longer present).
- `adb -s 192.168.1.12:37635 logcat -d | rg \"Couldn't register the navigator|FATAL EXCEPTION|E AndroidRuntime|ReactNativeJS\"` → no navigator crash entries after fix (only normal `Running \"main\"` marker).
- `node .yarn/releases/yarn-4.5.0.cjs demo:auto-solve` → release build/install + deep-link launch pass; run still times out waiting for demo auto-sequence token in logcat.

## Files Modified
- `docs/delivery/30/tasks.md`
- `docs/delivery/30/30-2.md`
- `docs/delivery/30/30-1-upgrade-findings.md`
- `docs/delivery/30/30-2-agent-device-guide.md`
- `package.json`
- `yarn.lock`
- `ios/Podfile.properties.json`
- `ios/Podfile.lock`
- `ios/Soli/AppDelegate.swift`
- `android/app/build.gradle`
- `scripts/run-demo-autosolve.js`
