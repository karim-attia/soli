[Back to task list](./tasks.md)

# [28-2] Speed up auto-up cadence + keep flights reliable

## Description
Increase auto-up (auto-complete) speed substantially by lowering the auto-queue delays, and ensure that at higher cadence, card-flight animations remain reliable (especially stock→waste draws which otherwise have no `Selection` context).

## Status History

| Timestamp | Event Type | From Status | To Status | Details | User |
| :-- | :-- | :-- | :-- | :-- | :-- |
| 2025-12-14 12:45:00 | Created | N/A | Proposed | Task created to make auto-up much faster while keeping flights reliable. | ai_agent |
| 2025-12-14 12:46:00 | Status Change | Proposed | Agreed | User requested much faster auto-up and asked to ensure animation reliability first. | user |
| 2025-12-14 12:52:00 | Status Change | Agreed | InProgress | Started implementing faster auto-up cadence and draw-step snapshot gating. | ai_agent |
| 2025-12-14 13:05:00 | Status Change | InProgress | Review | Reduced auto-up delays and added snapshot gating for auto-queue draw/recycle steps. | ai_agent |
| 2025-12-14 13:25:00 | Status Change | Review | InProgress | Reopened: auto-up still perceived slow; implement batching (multi-advance per tick) and re-verify timer behavior on-device. | ai_agent |
| 2025-12-14 13:35:00 | Status Change | InProgress | Review | Implemented multi-advance batching in reducer so auto-up progresses multiple actions per tick. | ai_agent |
| 2026-03-03 01:24:03 | Status Change | Review | InProgress | Reopened: reduce perceived delay between auto-up completion and visible victory motion start. | ai_agent |
| 2026-03-03 01:24:03 | Status Change | InProgress | Review | Increased celebration launch ramp speed so victory animation starts moving sooner after win. | ai_agent |
| 2026-03-03 01:36:54 | Status Change | Review | InProgress | Reopened: celebration still appears stacked on four kings at start; make initial breakout visibly immediate. | ai_agent |
| 2026-03-03 01:36:54 | Status Change | InProgress | Review | Added launch initial-progress floor and steeper launch ramp so cards separate from foundations immediately. | ai_agent |
| 2026-03-23 10:39:56 | significant_update | Review | InProgress | Reopened: celebration starts before the final foundation flight/glow completes; delay win handoff until the last foundation arrival animation has finished. | ai_agent |
| 2026-03-23 10:58:57 | Status Change | InProgress | Review | Delayed celebration start by the final move handoff window, then verified compile/lint plus an iOS simulator run that reached the win celebration with user-assisted reveal taps. | ai_agent |

## Requirements
- Reduce auto-queue delay values so auto-up runs much faster.
- Ensure auto-queue draw steps (stock→waste) remain snapshot-gated so the drawn card’s flight uses the correct origin.
- Keep the change scoped to auto-up; do not speed up manual interactions.
- Reduce perceived handoff delay between auto-up completion and celebration motion onset.
- Remove the initial “stacked kings” look by making celebration card motion visibly start immediately.
- Start the celebration only after the final winning card has visually arrived on its foundation pile so the last flight and yellow foundation glow do not overlap with the celebration.

## Implementation Plan
1. Add auto-up timing constants and wire them into `useAutoQueueRunner` from `useKlondikeGame`.
2. Ensure `dispatchWithFlight` snapshot-gates `ADVANCE_AUTO_QUEUE` draw steps by inferring the affected card id.
3. Validate the sequence via manual play and demo auto-solve.
4. Tune celebration launch ramp so motion starts earlier without changing overall celebration architecture.
5. Add a launch initial-progress floor so separation from the foundation stacks is visible on the first celebration frames.
6. Gate the celebration start behind the final foundation-arrival handoff so the winning move finishes visually before celebration motion begins.

## Test Plan
- Manual:
  - Trigger auto-up and verify it completes substantially faster.
  - Watch for bogus “off-screen” draw flights during auto-up.
  - Verify celebration cards begin moving promptly when the auto-up win state is reached.
  - Finish a game manually and confirm the last foundation flight and yellow glow complete before the celebration begins.
- Dev:
  - Run `yarn demo:auto-solve` and observe cadence/visual coherence.

## Verification
- Code: Auto-queue cadence increased via reduced delays in `useKlondikeGame`.
- Code: `ADVANCE_AUTO_QUEUE` draw/recycle steps now infer card IDs for snapshot gating (prevents off-screen stock→waste origins).
- Code: `ADVANCE_AUTO_QUEUE` now consumes multiple queued actions per tick so multiple flights can animate concurrently (dramatic speed-up).
- Code: `computeCelebrationFrame` now uses a faster launch-specific multiplier so victory motion starts sooner after `hasWon`.
- Code: `computeCelebrationFrame` now applies an initial launch-progress floor so celebration cards break out of stacked foundation positions immediately.
- Code: `useCelebrationController` now queues the win celebration and waits through the final card-flight/foundation-glow handoff window before starting celebration motion.
- Dev: `yarn exec tsc --noEmit` passes after this update.
- Dev: `yarn lint` passes after this update.
- Simulator: iPhone 17 Pro simulator launched `Soli`; after the final reveal taps (user-assisted), auto-up progressed to a win and the celebration dialog/animation state appeared successfully.

## Files Modified
- `docs/delivery/28/tasks.md`
- `docs/delivery/28/28-2.md`
- `src/features/klondike/constants.ts`
- `src/features/klondike/hooks/useCelebrationController.ts`
