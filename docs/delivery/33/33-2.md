[Back to task list](./tasks.md)

# [33-2] E2E CoS Test: exact card font parity

## Description
Verify the new bundled card visuals on the connected Android target and the booted iPhone simulator, then record whether the cards preserve the approved Android look while leaving the rest of the UI unchanged.

## Status History

| Timestamp | Event Type | From Status | To Status | Details | User |
| :-- | :-- | :-- | :-- | :-- | :-- |
| 2026-03-11 18:20:00 | Created | N/A | Proposed | Task created for end-to-end card font parity verification. | ai_agent |
| 2026-03-11 18:20:10 | user_approves | Proposed | Agreed | User requested direct validation on connected devices as part of the same implementation pass. | user |
| 2026-03-11 18:28:40 | start_work | Agreed | InProgress | Began live-device validation on the Android emulator and booted iPhone simulator after the card font assets were wired in. | ai_agent |
| 2026-03-11 18:31:00 | submit_for_review | InProgress | Review | Captured live Android and iOS board screenshots, confirmed the app remained on the play board, and inspected the visible card glyphs after the font change. | ai_agent |
| 2026-03-11 18:45:00 | significant_update | Review | InProgress | Reopened because the physical Android phone and iOS simulator still showed undersized suit symbols compared with the approved Android reference screenshot. | user |
| 2026-03-11 18:58:00 | submit_for_review | InProgress | Review | Revalidated the updated suit-glyph rendering on the physical Android phone and iPhone simulator after trimming the Android-derived glyph bounds. | ai_agent |
| 2026-03-11 19:37:46 | Verification Update | Review | Review | Recorded that the final shipping suit renderer now uses Android `NotoColorEmoji` exports and that the latest iOS simulator review is positive. | ai_agent |

## Requirements
- Verify card rank and suit glyph rendering on the connected Android target.
- Verify the same bundled card visuals render on the booted iPhone simulator.
- Confirm the rest of the app typography remains unchanged.
- Record the commands or artifacts used for the visual checks.

## Implementation Plan
1. Launch the latest app build on the Android target and navigate to the play board.
2. Capture the card visuals and compare them against the approved Android reference look.
3. Launch the latest app build on the iPhone simulator and confirm the same card glyphs render there.
4. Record the visual verification evidence and final task outcome.

## Test Plan
- Use mobile automation or platform screenshots to inspect the play board on both targets.
- Confirm that top-left ranks, top-right suits, and center suits all use the intended bundled glyphs.
- Confirm that header, buttons, and stats still use the existing app fonts.

## Verification
- Android target:
  - `adb -s 'adb-4139951e-zmJeED (2)._adb-tls-connect._tcp' shell dumpsys activity activities | rg -n 'mResumedActivity|topResumedActivity|ch.karimattia.soli'` confirms `ch.karimattia.soli/.MainActivity` is resumed on the physical phone.
  - Raw phone screenshot APIs returned blank frames, so verification switched to `adb ... shell screenrecord --time-limit 2 /sdcard/soli-phone-v6.mp4` plus local frame extraction with `imageio`, producing `/tmp/soli-phone-v6-frame0.png`.
- iOS simulator:
  - `xcrun simctl launch booted ch.karimattia.soli` launched the app on the booted `iPhone 17 Pro` simulator and returned PID `65731`.
  - `xcrun simctl io booted screenshot /tmp/soli-ios-card-font-check-v4.png` captured the final live board.
- Visual spot check:
  - Terminal crop inspection of the visible `5♠` card on `/tmp/soli-phone-v6-frame0.png` and `/tmp/soli-ios-card-font-check-v4.png` established the card scale and placement baseline before the final heart-shape correction.
  - The final suit renderer now uses checked-in Android `NotoColorEmoji` exports for all four suits, which replaced the shallower symbol-font/SVG path and produced the desired fat-heart shape on the booted iOS simulator during the latest review.
  - The header, stats, and other non-card UI text remain on the existing app fonts.

## Files Modified
- `docs/delivery/backlog.md`
- `docs/delivery/33/tasks.md`
- `docs/delivery/33/33-2.md`
